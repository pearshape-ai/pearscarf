[project]
name = "pearscaff"
dynamic = ["version"]
description = "Operational infrastructure that grows itself"
requires-python = ">=3.12"
dependencies = [
    "anthropic",
    "click",
    "discord.py",
    "httpx",
    "playwright",
    "python-dotenv",
]

[project.scripts]
pearscaff = "pearscaff.cli:cli"
ps = "pearscaff.cli:cli"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.version]
path = "pearscaff/__init__.py"
