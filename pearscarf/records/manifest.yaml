name: records
version: 0.1.1
source_type: mcp
author: pearshape-ai
description: Default record-capture expert. Records arrive via MCP submission; pearscarf labels, entity-resolves, and writes the graph. No ingester — submissions are push-based, not polled.

relevancy_check: skip

record_types: []
schemas: {}

new_entity_types: []
identifier_patterns: []

# Internal expert: prompts live under pearscarf/knowledge/, not co-located with the package.
knowledge: ../knowledge/records

# dedup_key, tools, eval, extraction — wired in later iterations.
# No `ingester` field — records is push-based via MCP, not polled.
