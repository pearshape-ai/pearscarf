# Gmail Extraction Knowledge (Layer 3)

Source-specific extraction guidance for Gmail records. Composed into the
extraction prompt at runtime when a record's source_type is `gmail`.

(stub — to be populated)
