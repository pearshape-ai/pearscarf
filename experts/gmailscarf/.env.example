# Gmail expert credentials — copy to env/.gmailscarf.env on install and fill in.
# Obtain OAuth credentials at https://console.cloud.google.com/apis/credentials

GMAIL_CLIENT_ID=
GMAIL_CLIENT_SECRET=
GMAIL_REFRESH_TOKEN=

# Polling interval (seconds). Optional — defaults to 300.
GMAIL_POLL_INTERVAL=300
