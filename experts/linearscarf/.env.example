# Linear expert credentials — copy to env/.linearscarf.env on install and fill in.
# Generate an API key at https://linear.app/settings/api

LINEAR_API_KEY=
LINEAR_TEAM_ID=

# Polling interval (seconds). Optional — defaults to 300.
LINEAR_POLL_INTERVAL=300
