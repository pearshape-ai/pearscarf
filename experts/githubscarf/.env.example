# GitHub personal access token — generate at https://github.com/settings/tokens
# Needs repo scope for private repos, or just public_repo for public ones.
GITHUB_TOKEN=

# Repository to poll (owner/repo format)
GITHUB_REPO=

# Polling interval (seconds). Optional — defaults to 300.
GITHUB_POLL_INTERVAL=300
