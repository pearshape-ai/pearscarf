# Plan: 1.2.3 — Gut Data Processing Logic

Clean slate for the new extraction pipeline. Stub out all data-writing and data-reading logic in the indexer, retriever, graph, vectorstore, worker triage, and memory CLI. Keep all infrastructure, wiring, module structure, and startup in place.

## Steps

### Step 1: Gut `pearscaff/indexer.py`

**`_extract` method** — empty the body, keep signature. Return empty dict.

**`_resolve_entity` method** — empty the body, keep signature. Return empty string.

**`_process_record` method** — keep `_build_content`, log entry, and `_mark_indexed`. Remove:
- LLM extraction call (Steps 1-4: `_extract`, entity resolution, edge creation, fact upserting)
- Qdrant embedding (Step 5)
- Associated log lines for extraction results, entity resolution, edges/facts written

The method becomes: log that it saw the record → build content (for future use) → mark indexed → log done.

**Keep intact:**
- `_build_content` — reads email table, useful for new extraction
- `_build_entity_types_block` — reads entity type registry
- `EXTRACTION_TEMPLATE` — comment it out as reference
- `__init__`, `_mark_indexed`, `_loop`, `start`, `stop` — unchanged
- `anthropic` import can stay (will be used again soon)

### Step 2: Gut `pearscaff/experts/retriever.py` tool bodies

Replace each tool's `execute` body with a stub return:

| Tool | Stub return |
|---|---|
| `SearchEntitiesTool.execute` | `"No results (extraction not yet implemented)"` |
| `FactsLookupTool.execute` | `"No facts (extraction not yet implemented)"` |
| `GraphTraverseTool.execute` | `"No connections (extraction not yet implemented)"` |
| `VectorSearchTool.execute` | `"No results (extraction not yet implemented)"` |

Keep: tool classes, schemas, descriptions, system prompt, factory function, all imports.

### Step 3: Gut `pearscaff/graph.py` function bodies

| Function | Stub return |
|---|---|
| `find_entity` | `return None` |
| `create_entity` | `return ""` |
| `get_entity` | `return None` |
| `search_entities` | `return []` |
| `create_edge` | `return ""` |
| `upsert_fact` | `return ""` |
| `get_entity_facts` | `return []` |
| `traverse_graph` | `return {"entities": [], "edges": [], "source_records": []}` |

**Keep intact:** `list_entity_types()` — fully functional (reads entity type registry).

Remove internal helpers that are now unreachable: `_next_entity_id`, `_next_edge_id`, `_next_fact_id`.

### Step 4: Gut `pearscaff/vectorstore.py` function bodies

| Function | Stub |
|---|---|
| `add_record` | No-op return |
| `query` | `return []` |

Keep: `_record_id_to_uuid`, `_get_client`, `_get_model`, `_embed`, `_ensure_collection`, constants, imports. These won't be called until functions are rebuilt.

### Step 5: Gut worker `SearchEntitiesTool` in `pearscaff/agents/worker.py`

Replace `SearchEntitiesTool.execute` body:
```python
return "No entities found (extraction not yet implemented)"
```

Simplify `WORKER_SYSTEM_PROMPT` triage section:
- Remove instruction to use `search_entities` for auto-classification (step 1 of triage)
- Remove the "If sender is a known entity" auto-classify path (step 2)
- Keep: present email to human and ask "is this relevant and why?", use `classify_record` to store decision, noise auto-classification for obvious signals
- The worker should still triage emails, but always asks the human (no graph-based auto-classify)

### Step 6: Gut `pearscaff/cli_memory.py` helper bodies

Replace each helper with a stub:

| Helper | Stub return |
|---|---|
| `_get_all` | `return []` (format_memory_list will show "No memories found.") |
| `_search` | `return []` |
| `_get_entity` | `return None` |
| `_graph_stats` | `return {"error": "Knowledge graph is being rebuilt. No data available."}` |
| `_get_memories_for_record` | `return []` |

The CLI commands and REPL `/memory` dispatch stay intact — they'll just show empty/stub messages via the existing format functions. No changes needed to `repl.py`.

### Step 7: Update `docs/architecture.md`

Add a note at the top of the "Knowledge Graph" section (before the entity_types bullet):

> **Note:** The extraction pipeline is in transition. The Indexer and Retriever are currently placeholder stubs — they start and run but do not extract or search. The graph tables and Qdrant collection remain in the schema for the upcoming extraction rebuild.

Don't remove any diagrams or sections.

### Step 8: Update `README.md`

Add a brief note in the Architecture section after the diagram, something like:

> **Note:** The knowledge graph extraction pipeline is being rebuilt. The Indexer currently marks records as indexed without extracting entities. The Retriever tools return empty results. Email reading, triage, and all agent communication work normally.

### Step 9: Update `CHANGELOG.md`

Add 1.2.3 entry:
```
## 1.2.3
- Gutted data processing logic in preparation for extraction pipeline rebuild
- Indexer: polls and marks records indexed, but no LLM extraction or embedding
- Retriever: tools registered but return empty results
- graph.py: all write/read functions stubbed (except list_entity_types)
- vectorstore.py: add_record and query stubbed
- Worker triage: simplified to always ask human (no graph-based auto-classify)
- Memory CLI/REPL: commands return stub messages
- No schema, dependency, or config changes
```

### Step 10: Bump version in `pearscaff/__init__.py`

`1.2.2` → `1.2.3`

## Execution prompt

```
Execute the 1.2.3 plan. Steps 1-10, in order. For each gutted function, keep the signature and docstring, replace the body with the stub return. Comment out the EXTRACTION_TEMPLATE in indexer.py. For graph.py, remove the _next_*_id helpers since they're unreachable. Simplify the worker triage prompt to remove graph-dependent auto-classification.
```
