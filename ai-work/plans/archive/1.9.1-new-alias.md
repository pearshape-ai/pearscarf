# Plan: 1.9.1 — CLI Alias `ps` → `psc`

Change the short CLI alias from `ps` (conflicts with macOS/Linux process status command) to `psc`. The full `pearscarf` command stays.

## File Changes

### 1. `pyproject.toml`

Line 25: `ps = "pearscarf.cli:cli"` → `psc = "pearscarf.cli:cli"`

### 2. `docs/usage.md`

Lines 36–43: Replace `ps memory` with `psc memory` in all command examples:

```
psc memory list
psc memory list --limit 20
psc memory list -f
psc memory list -f --interval 5
psc memory search "query"
psc memory entity "name"
psc memory graph
psc memory record <id>
```

### 3. `docs/architecture.md`

Line 337: `ps --version` → `psc --version`

### 4. `CHANGELOG.md`

Update historical references to the `ps` command:
- Line 76: `ps memory entity` → `psc memory entity`
- Line 77: `ps memory graph` → `psc memory graph`
- Line 78: `ps memory record` → `psc memory record`
- Line 83: `ps run` → `psc run`, `ps discord` → `psc discord`
- Line 219: `ps memory list/search/entity/graph/record` → `psc memory ...`
- Line 220: `ps memory list -f` → `psc memory list -f`
- Line 284: `ps --version` → `psc --version`

### 5. `pearscarf/__init__.py`

Bump version to `1.9.1`.

### 6. `CHANGELOG.md`

Add 1.9.1 entry:

```
## 1.9.1
- CLI short alias changed from `ps` to `psc` — avoids conflict with macOS/Linux `ps` (process status) command
- Full `pearscarf` command unchanged
- After updating: run `pip install -e .` (or `uv pip install -e .`) to register new entry point
```

## What does NOT change

- `pearscarf` entry point — stays as primary command
- No Python code changes
- No functional changes
- README.md — already uses `psc` (was updated during rename)

## Verification

1. `uv pip install -e .` to register new entry points
2. `psc --version` → `PearScarf v1.9.1`
3. `pearscarf --version` → same
4. `which psc` → points to `.venv/bin/psc`
5. `.venv/bin/ps` should no longer exist
6. `grep -rn "ps " docs/ --include="*.md" | grep -v psc | grep -v steps | grep -v https` — should return nothing

## Execution Prompt

```
Read ai-work/plans/1.9.1-new-alias.md and execute it. Bump version to 1.9.1, update CHANGELOG.md.
```
