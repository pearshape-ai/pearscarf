# Plan: 1.6.4 — Startup Version Banner

Print `PearScaff vX.Y.Z` as the first line when `ps run` or `ps discord` starts.

## File Changes

### 1. `pearscaff/cli.py`
Add version banner as first line in the `run` command, before any expert startup messages.

### 2. `pearscaff/discord_bot.py`
Add version banner as first line in `run_bot`, before any expert startup messages.

Both use `from pearscaff import __version__`.
