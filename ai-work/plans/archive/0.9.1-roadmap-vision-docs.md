# Plan 0.9.1: Vision, Roadmap, and Architecture Diagrams
**Date:** 2026-03-03
**Status:** Draft

## Context

Architecture, usage, getting-started, and README docs exist and are up to date through 0.9.0. Missing: `docs/vision.md`, proper roadmap with future items, and diagrams in `docs/architecture.md`. This is a documentation-only version — no code changes.

## Files

```
docs/
├── vision.md              # NEW: thesis, learning loop, explicit/implicit retrieval, multi-expert future
├── roadmap.md             # MODIFIED: replace current with spec's canonical checklist (completed + next + backlog)
├── architecture.md        # MODIFIED: prepend system diagrams (full system, email pipeline, retriever flow)
pearscaff/__init__.py      # MODIFIED: bump to 0.9.1
```

## `docs/vision.md` — New file

The spec provides the full content. Covers:
- **Thesis**: MCPs are transitional, expert agents navigating UIs via headless browsers are the future
- **How the system learns**: HIL triage, graph growth, auto-classification improvement, human context enrichment
- **Retrieval**: explicit (v0.9.0) vs implicit (future)
- **Multi-expert future**: Linear, Calendar, CRM experts all writing to shared graph
- **Always-on deployment**: Mac Mini / cloud target state

Copy directly from spec — it's well-written and complete.

## `docs/roadmap.md` — Replace current content

The current roadmap only has the "Completed" section. The spec provides a richer version with:
- **Completed**: all versions through 0.9.1
- **Next**: v0.10.0 through v0.13.0
- **Backlog**: batch loading, entity resolution, retriever caching, etc.
- **Exploratory**: third+ experts, A2A protocol, blog post

Replace the current roadmap entirely with the spec's version.

## `docs/architecture.md` — Prepend diagrams

Add three ASCII diagrams at the top of architecture.md, BEFORE the existing "Overview" section. Don't rewrite existing content.

1. **Full System Diagram** — Human → Worker → Experts + Indexer → Storage layers
2. **Email Pipeline Diagram** — Gmail Expert → Triage → Indexer → Graph + ChromaDB
3. **Retriever Query Flow** — Entity match → Facts → Graph traverse → Vector search → Context package

All diagrams provided in the spec.

## Implementation Steps

### 1. Create `docs/vision.md`
- Copy content from spec verbatim

### 2. Replace `docs/roadmap.md`
- Replace with spec's canonical checklist

### 3. Prepend diagrams to `docs/architecture.md`
- Add the three ASCII diagrams before the "# Architecture" heading
- Keep all existing content intact

### 4. Bump version to 0.9.1

## Verification

```bash
# Check all doc files exist and have content:
wc -l docs/vision.md docs/roadmap.md docs/architecture.md

# Verify no code changes:
git diff --stat -- '*.py'
# Should only show __init__.py (version bump)
```

## Execution Prompt

```
Execute plan 0.9.1: Create docs/vision.md from spec. Replace docs/roadmap.md with
spec's canonical checklist. Prepend three ASCII diagrams to docs/architecture.md.
Bump to 0.9.1.
```
