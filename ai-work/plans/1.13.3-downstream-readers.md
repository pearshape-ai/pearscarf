version: 1.13.3

# Plan: Downstream Readers Updated to New Fact Model Field Names

Field name update pass across all readers: `category` → `edge_label`+`fact_type`, `valid_at` → `source_at`, `invalid_at` → `stale`, `include_invalid` → `include_stale`, `include_historical` → `include_stale`, `categories` → `edge_labels`. No logic changes.

---

## Files

| Action | File | Purpose |
|--------|------|---------|
| Modify | `pearscarf/scoring.py` | Update match_facts and temporal_accuracy |
| Modify | `pearscarf/eval_runner.py` | Update _build_extracted_from_graph and _print_verbose_graph |
| Modify | `pearscarf/cli_memory.py` | Update format_entity, format_graph_stats, format_record_memories, _get_entity |
| Modify | `pearscarf/experts/retriever.py` | Update FactsLookupTool, GraphTraverseTool, DayLookupTool |
| Modify | `pearscarf/prompts/retriever.md` | Update fact descriptions, temporal markers, tool guidance |
| Modify | `pearscarf/__init__.py` | Bump to `1.13.3` |

---

## 1. `pearscarf/scoring.py`

### match_facts (lines 46-75)

Match key: `edge_label` + `fact_type` + `from_entity` + `to_entity` (replacing `category` + `from_entity` + `to_entity`).

Replace:
- `ext.get("category", "").upper()` → `ext.get("edge_label", "").upper()`
- `exp.get("category", "").upper()` → `exp.get("edge_label", "").upper()`
- Add `fact_type` comparison: `ext.get("fact_type", "").lower() == exp.get("fact_type", "").lower()`
- Update comparison: all four must match
- Update docstring

### temporal_accuracy (lines 156-191)

- Docstring: `valid_at` → `source_at`; remove `invalid_at`
- `assertion["fact_category"]` → `assertion["fact_category"]` (unchanged — ground truth field name)
- `fact.get("category", "")` → `fact.get("edge_label", "")`
- `fact.get("valid_at")` → `fact.get("source_at")`
- `assertion.get("valid_at")` stays (ground truth field)

---

## 2. `pearscarf/eval_runner.py`

### _build_extracted_from_graph (lines 94-133)

- `item.get("category", "")` → `item.get("edge_label", "")`
- Fact dict: `"category": category` → `"edge_label": edge_label, "fact_type": item.get("fact_type", "")`
- `"valid_at": item.get("valid_at", "")` → `"source_at": item.get("source_at", "")`

### _print_verbose_graph (lines 144-178)

Expected facts section (lines 162-166): keep as-is — these come from ground truth which may still use old field names.

Graph facts section (lines 172-176):
- `f.get("category", "?")` → `f"{f.get('edge_label', '?')}/{f.get('fact_type', '?')}"`
- `f['valid_at']` / `f.get("valid_at")` → `f['source_at']` / `f.get("source_at")`

---

## 3. `pearscarf/cli_memory.py`

### format_entity (lines 63-103)

Facts display (lines 83-91):
- `f.get("invalid_at")` → `f.get("stale")`
- `[was]` marker when `stale=True`
- `f.get('valid_at', '?')` → `f.get('source_at', '?')`
- Remove `-> invalid_at` range display
- `f['attribute']` → `f['edge_label']` + `/` + `f['fact_type']`

### format_graph_stats (lines 106-135)

- `stats.get("category_counts", {})` → `stats.get("edge_label_counts", {})`
- `"Fact categories:"` → `"Edge labels:"`
- Add `fact_type_counts` display

### format_record_memories (lines 138-154)

- `m.get("invalid_at")` → `m.get("stale")`
- `[was]` marker when `stale=True`
- `m.get('valid_at', '?')` → `m.get('source_at', '?')`
- `m.get("category", "?")` → `f"{m.get('edge_label', '?')}/{m.get('fact_type', '?')}"`

### _get_entity (lines 201-242)

- `graph.get_facts_for_entity(eid, include_invalid=True)` → `graph.get_facts_for_entity(eid, include_stale=True)`
- Fact dict mapping:
  - `"attribute": f.get("category", "?")` → `"edge_label": f.get("edge_label", "?"), "fact_type": f.get("fact_type", "")`
  - `"valid_at": f.get("valid_at", "")` → `"source_at": f.get("source_at", "")`
  - `"invalid_at": f.get("invalid_at")` → `"stale": f.get("stale", False)`
- Connections: `edge["category"]` → `edge["edge_label"]`

---

## 4. `pearscarf/experts/retriever.py`

### FactsLookupTool (lines 62-110)

- Description: "grouped by category" → "grouped by edge label"
- `include_historical` → `include_stale` (param name, schema, description)
- `graph.get_facts_for_entity(entity_id, include_invalid=include_historical)` → `graph.get_facts_for_entity(entity_id, include_stale=include_stale)`
- Group by `f["edge_label"]` instead of `f["category"]`
- Temporal display: `invalid_at`/`valid_at` → `stale`/`source_at`:
  ```python
  if f.get("stale"):
      temporal = " [stale]"
  elif f.get("source_at"):
      temporal = f" [since: {f['source_at']}]"
  ```

### GraphTraverseTool (lines 113-181)

- Description: "Returns categories" → "Returns edge labels"
- `include_historical` → `include_stale` (param name, schema, description)
- `categories` → `edge_labels` (param name, schema, description)
- `graph.traverse_fact_edges(..., categories=categories)` → `graph.traverse_fact_edges(..., edge_labels=edge_labels)`
- Edge temporal display: same `stale`/`source_at` pattern
- `edge['category']` → `edge['edge_label']`

### DayLookupTool (lines 184-216)

- `f['category']` → `f['edge_label']`

---

## 5. `pearscarf/prompts/retriever.md`

Full rewrite of the fact-related sections:

```markdown
The knowledge graph stores entities (people, companies, projects, events) connected by fact-edges. Three edge labels: AFFILIATED (organizational attachments), ASSERTED (claims, commitments, decisions), TRANSITIONED (state changes). Each edge carries a fact_type sub-label. Single-entity facts are anchored to Day nodes.
```

### Understanding fact edge labels (replaces "Understanding fact categories")

```markdown
## Understanding fact edge labels

- **AFFILIATED** (employee, founder, owner, contributor, ...) — stable organizational context
- **ASSERTED** (commitment, decision, blocker, evaluation, ...) — business claims with temporal significance
- **TRANSITIONED** (status_change, completion, cancellation, ...) — observed state changes
- **IDENTIFIED_AS** — system-only alias resolution, not shown in query results
```

### Temporal markers

```markdown
## Temporal markers

- `[since: <source_at>]` — fact recorded from a record at that time
- `[stale]` — fact has been superseded by a newer version
- By default tools return only current (non-stale) facts. Use `include_stale=true` for full history.
```

### Tool selection

Update `edge_labels` reference for GraphTraverseTool.

### Reply section

`grouped by category` → `grouped by edge label`

---

## 6. `pearscarf/__init__.py`

```python
__version__ = "1.13.3"
```

---

## Verification

1. `psc memory entity "James Whitfield"` → shows facts with edge_label/fact_type, source_at, stale markers.
2. `psc memory graph` → shows edge_label_counts and fact_type_counts instead of category_counts.
3. `psc eval --dataset <path> -v` → verbose output shows edge_label/fact_type for graph facts.
4. System is fully functional end-to-end after this spec.

---

## Execution prompt

```
Read the plan at ai-work/plans/1.13.3-downstream-readers.md and the spec at ai-work/specs/1.13.3-downstream-readers.md. Implement the plan exactly as written. This is a field name update pass only — no logic changes. Modify in order: scoring.py, eval_runner.py, cli_memory.py, experts/retriever.py, prompts/retriever.md, __init__.py. After implementation, run python -c "from pearscarf.scoring import match_facts; from pearscarf.eval_runner import run_graph_eval; print('OK')" to verify.
```
