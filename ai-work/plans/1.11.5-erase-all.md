version: 1.11.5

# Plan: Erase-All Script

New `scripts/erase_all.py` and `psc erase-all` CLI command that wipes all system state: Postgres records, Neo4j graph, Qdrant vectors. Confirmation required.

---

## Files

| Action | File | Purpose |
|--------|------|---------|
| Create | `scripts/erase_all.py` | Full system wipe script |
| Modify | `pearscarf/cli.py` | Add `psc erase-all` command |
| Modify | `pearscarf/__init__.py` | Bump to `1.11.5` |
| Modify | `docs/usage.md` | Add script and CLI command |
| Modify | `README.md` | Add `psc erase-all` to commands list |

---

## Verification

1. `psc erase-all` → shows counts, prompts `[y/N]`, wipes on `y`.
2. `python scripts/erase_all.py` → same behavior.
3. After wipe: `psc memory graph` shows zero entities, Postgres has zero records.

---

## Execution prompt

```
Implement the plan at ai-work/plans/1.11.5-erase-all.md.
```
