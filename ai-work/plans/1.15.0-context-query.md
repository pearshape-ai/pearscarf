version: 1.15.0

# Plan: context_query.py — Single Data Access Layer + Retriever Rewire

Creates `context_query.py` as the read-only data access layer. Adds `get_path` and `get_conflicts` to `graph.py`, `get_communications_for_entity` to `store.py`. Rewires all retriever tools to call through `context_query`.

---

## Files

| Action | File | Purpose |
|--------|------|---------|
| Modify | `pearscarf/graph.py` | Add `get_path()` and `get_conflicts()` |
| Modify | `pearscarf/store.py` | Add `get_communications_for_entity()` |
| Create | `pearscarf/context_query.py` | Read-only data access layer |
| Modify | `pearscarf/experts/retriever.py` | Rewire all tools to use context_query |
| Modify | `pearscarf/__init__.py` | Bump to `1.15.0` |

---

## Execution prompt

```
Implement the plan exactly as written.
```
