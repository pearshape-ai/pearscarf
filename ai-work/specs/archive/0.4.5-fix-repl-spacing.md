version: 0.4.5

# Context

- Response text alignment in the REPL is still broken despite v0.4.4.
- Text is scattered across the terminal with erratic indentation instead of being left-aligned.

# Problem

Worker responses render with random horizontal offsets. Every line starts at a different column position. The text should be flush left-aligned, consistently.

## Current (broken):

```
[ses_009] worker > **Latest Unread Email (Now Marked as Read)**
                                                                                  
                                                                                                     **From:** Google <no-reply@accounts.google.com>  
                       **Subject:** Security alert  
                                                                       **Date:** Feb 28, 2026, 6:40 PM
```

## Expected:

```
[ses_009] worker > **Latest Unread Email (Now Marked as Read)**

**From:** Google <no-reply@accounts.google.com>
**Subject:** Security alert
**Date:** Feb 28, 2026, 6:40 PM

**Body:**
A new sign-in on Mac gevorg@aimhub.io We noticed a new sign-in to your Google
Account on a Mac device. If this was you, you don't need to do anything.
```

# TODO

- Strip all leading whitespace from each line of agent responses before printing.
- After the first `[ses_XXX] agent >` prefix line, all continuation lines start at column 0.
- Ensure no padding, indentation, or column-alignment logic is applied to response text.
- Check if the issue is in how the message is received from the LLM (extra whitespace in the response) or in how the REPL renders it (adding padding). Fix whichever is the cause.

# Root cause hint

This is likely one of:
1. The REPL is padding continuation lines to align with the prefix width — it shouldn't.
2. The LLM response itself contains whitespace or formatting characters — strip them on output.
3. Some print/display function is applying column-based formatting — remove it.