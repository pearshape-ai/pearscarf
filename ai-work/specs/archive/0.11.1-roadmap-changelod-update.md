version: 0.11.1

# Context

- Gmail MCP integration is live and working.
- The granular checkbox roadmap served its purpose during core system buildout but is now noise.
- Need to restructure docs: roadmap becomes high-level milestones, completed work becomes a changelog.

# TODO

## 1. Replace docs/roadmap.md with high-level milestones

No version numbers. No checkboxes. Intentions and direction.

```markdown
# Roadmap

## Now: Storage & Context

The agent communication layer works. Triage works. The pipeline works. What needs to get right is the quality of stored context — how well heterogeneous operational data gets connected, stored, and surfaced to models when they need it.

Evaluating memory backends (Mem0, Graphiti, Cognee) as a pluggable storage layer behind the Retriever. Goal: integrate all three with an on/off switch, test against real email data, and let usage decide which one wins.

## TODO

- Linear expert agent via MCP — second data source, validate cross-source entity connection
- Cloud deployment — always-on system running on Mac Mini or cloud, Gmail polling, Discord as the human interface
- Implicit retrieval — worker auto-retrieves context before responding, without the human asking
- Observability — LangSmith or Langfuse for trace visualization, cost tracking, and evaluation
- Additional expert agents (Calendar, CRM) as the pattern stabilizes
```

## 2. Create CHANGELOG.md in the project root

Not in docs/. Standard project changelog in the main directory.

```markdown
# Changelog

## 0.11.0
- Gmail expert MCP integration (OAuth, API-based email operations)
- Email polling loop with --poll-email flag (configurable interval)
- New email notifications on Discord and REPL
- MCP as default transport when configured, headless browser as fallback
- pearscaff gmail --auth command for OAuth setup

## 0.10.0
- Roadmap and vision docs update
- Balanced vision framing (transport-agnostic expert architecture)

## 0.9.1
- Project documentation: architecture diagrams, vision, roadmap, getting-started

## 0.9.0
- Retriever agent (explicit context queries)
- Three query modes: facts lookup, graph traversal, vector search
- Structured context packages returned to worker

## 0.8.0
- HIL triage (auto-classify or ask human)
- Human context capture during triage (fed to Indexer)
- Classification override support
- All classification activity visible on Discord and REPL

## 0.7.0
- ChromaDB integration (vector embeddings with sentence-transformers)
- Indexer embeds record content for semantic search

## 0.6.0
- Knowledge graph (entities, edges, facts, entity_types registry)
- Indexer agent (background LLM extraction into graph)

## 0.5.0
- System of Record (expert-owned storage, email deduplication)

## 0.4.0
- Unified session logging (actions, tool calls, reasoning, thinking, errors)
- Versioning (ps --version)
- REPL UX improvements

## 0.3.0
- Session-based async communication via SQLite
- Terminal REPL with session management
- Discord bot with thread-per-session mapping

## 0.2.0
- Worker agent with reasoning and task routing

## 0.1.0
- Gmail expert agent (headless browser, reads emails, marks as read)
```

## 3. Update docs/vision.md

If not already updated from 0.10.0, ensure vision.md reflects the balanced framing:

- Separation of concerns is the core principle
- Transport layer is an implementation detail per expert
- The context layer is the differentiator
- Remove any strong anti-MCP language

## 4. Clean up old spec references

Remove the "Next", "Backlog", "Exploratory" checkbox sections from any existing docs. The roadmap is now prose, not tasks. Claude Code updates the changelog when it completes work. New roadmap items come from planning conversations.

## What this spec produces

- Rewritten docs/roadmap.md (high-level milestones)
- New CHANGELOG.md in project root (factual record)
- Updated docs/vision.md if needed
- No code changes