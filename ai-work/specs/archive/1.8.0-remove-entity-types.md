version: 1.8.0

# Context

- Beginning of the graph architecture changes. Facts become edges, Day nodes get introduced, entity types are hardcoded in the extraction prompt. Cleaning dead code before building the new model.
- `list_entity_types()` in `graph.py` reads from a Postgres `entity_types` table — a registry that was used to dynamically build the `{entity_types_block}` placeholder in the old extraction prompt template.
- Since v1.3.x, the extraction prompt lives in `pearscaff/prompts/extraction.md` as standalone markdown. Entity types (person, company, project, event) are written directly in the prompt. No dynamic substitution, no database lookup.
- The indexer was rewritten and no longer calls `list_entity_types` or `_build_entity_types_block`.
- Nothing in the codebase calls `list_entity_types`. It's dead code.

# TODO

## Remove `list_entity_types` from `pearscaff/graph.py`

- Delete the `list_entity_types()` function and its docstring
- Delete the `# --- Entity types (Postgres) ---` section comment above it
- Remove the `from pearscaff.db import _get_conn, init_db` import line entirely — no remaining function in graph.py uses `_get_conn` or `init_db` (all functions use Neo4j via `get_session`)
- Update the module docstring at the top: remove "list_entity_types() still reads from Postgres."

## Remove `entity_types` table and seed from `pearscaff/db.py`

- Delete the `entity_types` CREATE TABLE block from `_SCHEMA`
- Delete the `_SEED_ENTITY_TYPES` constant entirely
- Delete the `conn.execute(_SEED_ENTITY_TYPES)` line from `init_db()`

## Remove `entity_types` from `docs/architecture.md`

- In the Database Schema SQL block, remove the `entity_types` table definition
- In the Knowledge Graph section, remove the bullet: "**`entity_types`** — Registry of extractable types (person, company). Seeded on first run. Drives the LLM extraction prompt."
- In the Indexer subsection, step 2 says "Builds extraction prompt from `entity_types` registry" — update to "Loads extraction prompt from `pearscaff/prompts/extraction.md`"

## Remove `entity_types` from `scripts/migrate_sqlite_to_postgres.py`

- Remove `"entity_types"` from the `TABLES` list
- Remove `"entity_types": ["extract_fields"]` from the `JSON_COLUMNS` dict

## Grep for stragglers

- Search the entire codebase for `entity_types`, `list_entity_types`, `_build_entity_types_block`, `entity_types_block`, `_SEED_ENTITY_TYPES`
- Remove any dead references found — imports, comments, docstrings

## What NOT to do

- No changes to Neo4j node labels — entity types as Neo4j labels (Person, Company, Project, Event) are unrelated to this Postgres table
- No changes to the extraction prompt
- No changes to any other `graph.py` functions
- No changes to `store.py`, `indexer.py`, or any agent code
- No new functionality — this is purely removal