version: 0.4.2

# Context

- Need a way to verify which version of PearScaff is running.
- Version should be defined in one place and accessible from the CLI.

# TODO

- Add a single source of truth for the version string (e.g. a config file, package.json, or a constant).
- Implement `ps --version` command that prints the current version and exits.
- Version format: semver (e.g. `0.4.2`).
- The REPL should also print the version on startup:
  ```
  PearScaff v0.4.2
  [ses_001] you > 
  ```