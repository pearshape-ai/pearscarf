version: 1.3.0

# Context

- Prompts are hardcoded as Python string constants across agent modules.
- The extraction prompt is about to go through heavy iteration — editing a prompt shouldn't require editing Python code.
- Prompts should be easy to read, edit, and diff independently of the codebase.

# TODO

## Extract all system prompts to markdown files

Create a `pearscaff/prompts/` folder. One flat markdown file per prompt — no subfolders, no wrappers, no frontmatter. The file content is the prompt.

Prompts to extract:
- Worker system prompt
- Gmail expert browser transport prompt
- Gmail expert MCP transport prompt
- Retriever system prompt
- Extraction template (the indexer's LLM prompt — has format-string placeholders for record content and entity types)

## Prompt loader

A utility that loads a prompt by name from the `pearscaff/prompts/` folder and returns it as a string. All modules that currently define prompt constants should use this loader instead.

Prompts with placeholders (like the extraction template) stay as Python format strings in the markdown — the calling code fills them in.

## What NOT to do

- No prompt content changes — move as-is
- No YAML/JSON wrappers
- No hot-reload or caching logic
- No subfolders