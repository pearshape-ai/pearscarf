version: 1.9.0

# Context

- Project is renaming from PearScaff to PearScarf. New name, new identity, new logo.
- This is a full rename across the entire codebase — module names, imports, config references, docs, prompts, Docker, CLI commands, Discord bot, everything.
- The repo name on GitHub changes last, after everything inside is updated and verified working.
- Logo: https://github.com/user-attachments/assets/ecaf3cc6-a8a1-4af9-a5ee-545b7e9d38ef

# TODO

## Python package rename

- Rename `pearscaff/` directory to `pearscarf/`
- Update `pyproject.toml`: package name, module references, entry points
- The CLI command changes from `ps` (if it references pearscaff internally) — verify the entry point name. If the CLI command is already `ps`, it can stay as `ps`.
- Update all internal imports across every Python file: `from pearscaff.` → `from pearscarf.`, `import pearscaff.` → `import pearscarf.`

## File-by-file import sweep

Every `.py` file in the project needs its imports updated. Key files:
- `pearscarf/__init__.py`
- `pearscarf/cli.py`
- `pearscarf/config.py`
- `pearscarf/db.py`
- `pearscarf/store.py`
- `pearscarf/graph.py`
- `pearscarf/neo4j_client.py`
- `pearscarf/vectorstore.py`
- `pearscarf/indexer.py`
- `pearscarf/extract_test.py`
- `pearscarf/tracing.py`
- `pearscarf/agents/worker.py`
- `pearscarf/experts/gmail.py`
- `pearscarf/experts/linear.py`
- `pearscarf/experts/expert.py`
- `pearscarf/linear_client.py`
- `pearscarf/discord_bot.py`
- `pearscarf/repl.py`
- Any other `.py` files in the tree

Use a global find-and-replace: `pearscaff` → `pearscarf` across all `.py` files. Then verify no broken imports.

## Prompts

- Update `pearscarf/prompts/` — if any prompt file references "PearScaff" by name (system prompts, extraction prompt), change to "PearScarf"
- Check: `worker.md`, `retriever.md`, `extraction.md`, `gmail-expert-browser.md`, `gmail-expert-mcp.md`, `linear.md`

## Docker

- `Dockerfile` — update any references to pearscaff (image name, module path, WORKDIR, CMD)
- `docker-compose.yml` — update service names, image names, container names if they reference pearscaff
- `.env.example` or any env template files

## Documentation

- `README.md` — all references to PearScaff → PearScarf, update logo, update repo URLs
- `docs/architecture.md` — all references
- `docs/usage.md` — all references
- `CHANGELOG.md` — all references, add 1.9.0 entry for the rename
- `CLAUDE.md` — if it references pearscaff
- Any other `.md` files

## Config and metadata

- `pyproject.toml` — name, description, URLs, package dir
- `.github/` — any workflow files referencing pearscaff
- `LICENSE` — if it mentions the project name

## LangSmith

- If the LangSmith project name is configured as "pearscaff" in config or tracing setup, update to "pearscarf"
- This affects trace organization in the LangSmith dashboard

## Discord bot

- If the bot name or any Discord-visible text says "PearScaff", update to "PearScarf"
- Check `discord_bot.py` for display name references

## Verification

After all renames:
- `grep -r "pearscaff" .` across the entire repo — should return zero results (except maybe CHANGELOG noting the rename)
- Run the test suite / extraction test script — verify imports work
- Start the system locally — verify CLI, Discord bot, indexer, retriever all start
- Check Neo4j browser — verify graph operations work (module rename shouldn't affect data, but verify)
- Check LangSmith — verify traces show up under the new project name

## Repo rename (last step, manual)

- Rename the GitHub repo from pearscaff to pearscarf
- Update any external references (blog post draft, product decisions doc, framework vision doc)
- Clone fresh with the new name

## What NOT to do

- No functional changes — this is purely a rename
- No schema changes, no graph changes, no extraction changes
- No data migration — Neo4j data, Postgres data, Qdrant data are all unaffected by a Python module rename
- Don't rename the CLI command if it's already short and generic (`ps`) — unless it's literally `pearscaff` as the command name