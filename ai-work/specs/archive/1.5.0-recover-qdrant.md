version: 1.5.0

# Context

- Neo4j is wired and working (v1.4.0). The Indexer extracts entities, relationships, and facts from emails and writes them to the graph.
- Qdrant is running in Docker, config is in place (QDRANT_URL), the `vectorstore.py` module has function signatures intact but stubbed bodies.
- The Retriever's `VectorSearchTool` is stubbed — it returns "not yet implemented."
- Vector search is the complement to graph queries. The graph answers "what do I know about Acme" (structured traversal). Vector search answers "anything about cost optimization" or "emails related to scaling" (semantic similarity). The Retriever needs both to be useful.

# TODO

## Wire vectorstore.py to Qdrant

Replace the stubbed function bodies with real Qdrant reads and writes. The module already has the lazy init helpers (`_get_client`, `_get_model`, `_embed`) and the interface (`add_record`, `query`). Unstub them.

`add_record` should embed the email content using the sentence-transformers model and upsert the vector + payload into Qdrant. Payload should include the record_id, content, and email metadata (sender, subject, type, source) so search results carry enough context to be useful without a second lookup.

`query` should embed the query text, search Qdrant, and return results with id, content, metadata, and distance.

## Wire the Indexer to embed after extraction

After the Indexer writes entities/relationships/facts to Neo4j, it should also embed the email content into Qdrant via `vectorstore.add_record`. Same content that gets passed to extraction — the assembled email text from `_build_content`.

This is an additional step in `_process_record`, not a replacement for anything. The flow becomes: build content → extract via LLM → write to Neo4j → embed in Qdrant → mark indexed.

## Wire the Retriever's VectorSearchTool

Unstub `VectorSearchTool.execute` to call `vectorstore.query`. The tool should return record IDs, content snippets, metadata, and similarity scores so the Retriever can assemble a useful context package combining graph results and vector results.

## Wire memory CLI vector search

Unstub `ps memory search` to use `vectorstore.query` for semantic search. `ps memory list` can also be unwired if it makes sense to show recent vectors, or it can stay focused on graph entities — decide during planning.

## What stays the same

- Neo4j wiring — untouched, already working
- Extraction prompt — no changes
- Graph.py — no changes
- All agent communication, Gmail expert, email polling — untouched
- Docker setup — Qdrant already running

## What NOT to do

- No changes to the extraction prompt or entity types
- No bi-temporal timestamps
- No new dependencies — qdrant-client and sentence-transformers are already installed