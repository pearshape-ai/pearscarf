version: 1.6.2

# Context

- Linear expert is working — interactive tools, polling, sync with comments, issues in the SOR.
- The Indexer already processes email records through extraction → Neo4j → Qdrant. It handles both email and issue record types in `_build_content` — the issue content builder is already implemented, assembling identifier, title, status, priority, assignee, project, labels, description, and comments into a text block.
- The extraction prompt works well on emails. Issues are a different shape — structured metadata fields plus threaded comments. The prompt may need tuning but the same four entity types (person, company, project, event) should apply.
- This is the cross-source entity resolution test. An email mentioning "Acme integration" and a Linear issue titled "Acme API Integration" should resolve to the same project entity in Neo4j through name normalization and entity matching.

# TODO

## Feed issues through the existing extraction pipeline

The Indexer already polls for unindexed relevant records and processes them. Issues classified as relevant should flow through the same path — extraction via LLM, entity resolution, Neo4j writes, Qdrant embedding. The `_build_content` method already handles issue records. The pipeline should work for issues without structural changes.

Verify that the Indexer correctly picks up issue records (type = "issue") alongside email records. If the polling query or any logic assumes email-only, adjust.

## Extraction prompt tuning for issues

Test the existing prompt against real Linear issues. Issues have structured metadata (status, priority, assignee) that's already explicit — the prompt shouldn't re-extract what's in the fields. The valuable extraction is from the description and comments: project references, person mentions, commitments, blockers, decisions.

The `{record_type}` is passed to the prompt as part of the user message. If the prompt needs issue-specific guidance (e.g. "for issues, focus on description and comments, not structured fields"), add a small conditional section to the prompt rather than a separate prompt file.

## Cross-source entity resolution verification

This is the critical test. Run extraction on issues that overlap with existing email-derived entities and verify:

- A person mentioned in emails and assigned to an issue resolves to the same Neo4j node
- A company discussed in emails and referenced in issue content resolves to the same node
- A project entity from email extraction matches one from issue extraction

The entity resolution in `graph.py` (name match + email/domain fallback via `find_entity` and `create_entity` with MERGE) handles this. The normalization guidance in the extraction prompt ensures names are consistent. Document any edge cases that appear.

## What NOT to do

- No new entity types for issues — same four types
- No separate extraction prompt for issues — one prompt, both sources
- No audit log or change history
- No changes to the Linear expert's interactive tools or polling