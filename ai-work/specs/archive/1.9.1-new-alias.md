version: 1.9.1

# Context

- The CLI command `ps` conflicts with the macOS/Linux `ps` (process status) command.
- Changing to `psc` — short, no common unix conflicts, easy to type.
- The full command `pearscarf` stays as the primary entry point. `psc` is the short alias.

# TODO

## Update `pyproject.toml`

- In `[project.scripts]`, change the `ps` entry to `psc`
- Keep `pearscarf` as the primary entry point
- Result: both `pearscarf run` and `psc run` work, `ps` no longer registered

## Update README.md

- Replace all `ps ` references with `psc `
- Update the "Also available as" line to reference `psc`

## Update docs

- `docs/usage.md` — replace `ps` with `psc` in all command examples
- `docs/getting-started.md` — same
- Any other doc files with `ps` command references

## Update REPL help text

- If the REPL displays help text or examples referencing `ps`, update to `psc`

## Update Discord bot

- If the bot displays help or command examples referencing `ps`, update to `psc`

## Grep for stragglers

- `grep -r '"ps "' .` and `grep -r "'ps "' .` across the codebase
- Be careful not to match partial strings like "https" or "steps" — search for `ps ` with trailing space or `"ps"` as a standalone token

## What NOT to do

- No functional changes — just the alias name
- Don't remove the `pearscarf` entry point — it stays as the full command
- Don't change any Python module names or imports