version: 1.8.6

# Context

- The graph now has entity nodes, Day nodes, and fact-edges with categories and temporal metadata.
- The indexer writes through `create_fact_edge` and `get_or_create_day` (v1.8.5).
- The retriever tools still query the old structure — `get_entity_facts`, `traverse_graph`, old edge shapes.
- `get_facts_for_entity` and `get_facts_for_day` exist in graph.py (v1.8.3).
- The retriever is the read side. It needs to understand the new edge shape to return useful context.

# TODO

## Rewire `FactsLookupTool`

- Currently calls old `get_entity_facts`
- Rewire to call `get_facts_for_entity(name, include_invalid=False)`
- Return format should include: category, fact text, confidence, valid_at, source_type, the other entity's name (or Day date if connected to a Day node)
- Group results by category for readability in the agent's context

## Rewire `GraphTraverseTool`

- Currently walks old relationship edges from a node
- Rewire to walk fact-edges from a given entity up to N hops
- Every edge in the traversal is a fact-edge — return category, fact text, connected entity name, confidence
- Filter to current facts by default (invalid_at is null)
- Day nodes appear naturally in traversal when single-entity facts are followed — include them in results but label them as Day nodes so the agent knows the difference

## Rewire `SearchEntitiesTool`

- Searches entity nodes by name/metadata — this mostly works as-is since entity nodes haven't changed
- If the search result format includes fact counts or connected entities, update those to reflect fact-edges instead of old edges

## `VectorSearchTool` — unchanged

- Qdrant search stays as-is, no changes needed

## Add `DayLookupTool` (new tool)

- Takes a date string (ISO format, e.g. "2026-03-21") or a natural language date ("yesterday", "last Monday") — the tool resolves to an ISO date before querying
- Calls `get_facts_for_day(date_string)`
- Returns all fact-edges anchored to that Day node: category, fact text, the entity connected, confidence, source info
- Register it alongside the other retriever tools

## Update retriever prompt (`pearscaff/prompts/retriever.md`)

Update tool descriptions to reflect the new data model. The retriever agent needs to understand:

- Facts are edges with categories — when it sees results, it should reason about the category (WORKS_AT is stable context, COMMITTED_TO is a time-sensitive claim, BLOCKED_BY is actionable)
- Day nodes exist — for "what happened on date X" queries, use DayLookupTool
- Current vs historical — tools return current facts by default. If the query asks about history ("what was the status last week"), the agent should note this limitation

Tool selection guidance for the prompt:
- Entity-specific queries ("what's going on with Acme") → FactsLookupTool on the entity, then GraphTraverseTool for connected entities
- Date-specific queries ("what happened March 13") → DayLookupTool, but note this only catches single-entity facts anchored to the Day node, not two-entity facts that happened that day
- Fuzzy queries ("anything about compliance delays") → VectorSearchTool first to find relevant records, then FactsLookupTool on entities found
- Entity search ("who is Michael Chen") → SearchEntitiesTool, then FactsLookupTool for their facts

## Remove old graph.py functions

If not already removed in v1.8.5:
- Remove `create_edge`
- Remove `upsert_fact`
- Remove `get_entity_facts`
- Remove `traverse_graph` (replaced by the new traversal that walks fact-edges)

Add a new `traverse_fact_edges` function if the old `traverse_graph` signature doesn't fit the new model. The traversal should:
- Start from a given entity node
- Walk outgoing and incoming fact-edges up to N hops
- Return entities and Day nodes encountered, with the fact-edges that connect them
- Filter to current facts by default (invalid_at is null)
- Optional category filter (e.g. only walk WORKS_AT and MANAGES edges)

## What NOT to do

- No extraction prompt changes
- No indexer changes
- No entity resolution changes
- No memory CLI updates — that's a separate version
- No display formatting — the retriever returns raw data, the worker/Discord formats it