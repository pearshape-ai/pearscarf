version: 0.2.0

# Context
This discord bot agent is to be turned into the main user-facing agent, called worker agent.
New kind of agent created called expert agent.

# TODO
- Create Gmail expert agent with its own loop, cli access, spinning up,

## Expert agent Functionalities
- Gmail expert agent is a headless browser agent that can log into Gmail, read emails, mark as read.
- Expert agent does it on command via cli
- Expert agent only reads the latest unread email, posts its contents and a summary to the terminal
- All interactions with the expert agent happen on a terminal.
- This agent is great at operating the Gmail API
    - This agent collects information about operating the gmail api, stores in a respective folder for its expert knowledge in a markdown.