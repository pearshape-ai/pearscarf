version: 0.0.0

# Context
The first version is to create the basic agent that can be used from the CLI.

# TODO
- Create a custom simple agentic-loop that runs on anthropic api, accepts the token from the env.
- Implement tool access and execution
- Implement and define what tools are
- Implement ttwo sample tools: math, web search
- The agent to be used from the CLI
- Create the architecture in a way that the agentic loop, tooling implementation is pluggable, so in the future something like langchain can be used to replace if need be.
    - propose an assessment whether this is good idea to give a choice for final decision
    - the alternative being just the implementation, without any pluggability considerations.
