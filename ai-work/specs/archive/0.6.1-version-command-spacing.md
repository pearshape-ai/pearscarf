version: 0.6.1

# Context

- `ps --version` output has a stray `%` character and inconsistent formatting.

# Problem

Current output:
```
PearScaff, version 0.6.0
                        % 
```

## TODO

- Ensure the version output ends with a newline so the shell prompt doesn't append `%`.
- Expected output:
```
PearScaff v0.6.1
```