version: 1.16.0

# Update eval scorer

## Context

- `score_record` returns one aggregate `fact_matched/extracted/expected` — failures in AFFILIATED vs ASSERTED vs TRANSITIONED are invisible in the output
- `temporal_accuracy` expects the old flat assertion format with `record_id`, `fact_category`, `from_entity`, `valid_at` — ground truth v1.0.1 uses nested `expected_edges` with `source_record`, `stale`, and `valid_until`
- `entity_resolution_accuracy` only handles `"merge"` and `"split"` outcomes — the new `"domain_inferred"` pair for email_027 (Sarah Chen) has no `surface_b`/`record_b` and requires checking for an AFFILIATED edge in the facts dict rather than comparing two entity names
- `match_entities`, `match_facts`, `precision`, `recall`, `f1`, `noise_rejection_rate` are correct and untouched

## TODO

- `score_record`: replace the `return` statement with a result dict; then extend it with per-label counts by running `match_facts` three times, once per edge label (AFFILIATED, ASSERTED, TRANSITIONED), using filtered slices of `ext_facts`/`exp_facts`; add `{label}_matched`, `{label}_extracted`, `{label}_expected` keys for each label
- `score_record`: after the per-label pass, scan for confidence mismatches — where identity fields match (edge_label, fact_type, from_entity, to_entity) but `confidence` differs; append a warning string per mismatch; add `confidence_warnings` list to the result dict; mismatches are informational only and do not affect match counts
- `temporal_accuracy`: replace the entire function — iterate `expected_edges` nested inside each assertion; for each edge look up facts by `source_record` in `extracted_facts_by_record`; match on edge_label + fact_type + from_entity; check `stale` matches expected value and `valid_until` date (first 10 chars) matches when present; `correct / total` where total is count of expected edges across all assertions
- `entity_resolution_accuracy`: add optional third parameter `extracted_facts_by_record=None`
- `entity_resolution_accuracy`: add a `domain_inferred` branch at the top of the for loop — look up facts from `extracted_facts_by_record[record_a]` and check for an AFFILIATED edge where `from_entity == surface_a` and `to_entity == canonical_entity`; `continue` past the merge/split logic after the branch

## What NOT to do

- Do not change `match_entities`, `match_facts`, `precision`, `recall`, `f1`, or `noise_rejection_rate`
- Do not fail a fact match when confidence mismatches — warnings only
- Do not add new parameters to `score_record` — per-label data comes from `ext_facts`/`exp_facts` already in scope